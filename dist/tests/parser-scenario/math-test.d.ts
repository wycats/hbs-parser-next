import type * as qunit from "qunit";
import { Parser } from "./tokens";
export declare class MathParserTest {
    #private;
    assert: qunit.Assert;
    parser: Parser;
    "plus without whitespace"(): void;
    "plus with whitespace"(): void;
    "paren (without whitespace)"(): void;
    private assertInvoke;
    private invoke;
}
//# sourceMappingURL=math-test.d.ts.map